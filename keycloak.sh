export DOMAIN_SUBDOMAIN=auth.elusian.net # for example mykeycloak.com or keycloak.mydomain.com
export KEYCLOAK_SSL_ALIAS=elusian # the alias for your SSL certificate which can be anything but avoid weird characters
export KEYCLOAK_SSL_PASSWORD=ggnorelusian123 # the password for your SSL certificate, again avoid weird characters
export KEYCLOAK_PASSWORD=ggnore123 # password for initial admin account